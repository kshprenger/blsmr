[workspace]
resolver = "3"
members = [
    "crates/*",
]

[workspace.dependencies]
dscale = {git = "https://codeberg.org/kshprenger/dscale.git", branch = "master"}

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
opt-level = 3
debug = true
