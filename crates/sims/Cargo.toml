[package]
name = "sims"
version = "0.1.0"
edition = "2024"

[dependencies]
dscale = {workspace = true, features = ["mpi"]}
blsmr = {path = "../blsmr"}
hotstuff = {path = "../hotstuff"}
bullshark = {path = "../bullshark"}
client = {path = "../client"}
quorum = {path = "../quorum"}
